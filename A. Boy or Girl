#include<iostream>
#include<vector>

using namespace std;

int main(){
    
    string s;
    cin>>s;

    vector<int> v(26,0);
    for(int i=0; i<s.size(); i++){
        v[s[i]-'a']=1;

    }

    int count=0;
    for(int i=0; i<26; i++){
        count+=v[i];
    }

    if(count % 2 != 0)
        cout << "IGNORE HIM!";
    else
        cout << "CHAT WITH HER!";


    
}
